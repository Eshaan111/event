import SubmitProposalClient from "./SubmitProposalClient";

export const metadata = { title: "New Proposal — Aetheric Studio" };

export default function NewProposalPage() {
  return <SubmitProposalClient />;
}
